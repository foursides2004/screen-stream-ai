{
  "name": "screen-stream-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Screen-reading AI assistant with Next.js web app and Python local client",
  "workspaces": [
    "backend-vercel"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend-vercel",
    "build": "npm run build --workspace=backend-vercel",
    "lint": "npm run lint --workspace=backend-vercel",
    "dev:client": "cd local-client && python capture_agent.py",
    "dev:all": "concurrently \"npm run dev\" \"npm run dev:client\"",
    "install:all": "npm install && cd backend-vercel && npm install && cd ../local-client && pip install -r requirements.txt",
    "build:prod": "npm run build --workspace=backend-vercel",
    "lint:all": "npm run lint --workspace=backend-vercel && cd local-client && ruff check . && ruff format --check ."
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.8.0"
}